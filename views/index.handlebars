{{#each items}}
    <div class="form-check">
        <input type="checkbox" value="" id="id" class="form-check-input"/>
        <label class="form-check-label" for="id">{{item.name}}</label>
    </div>
{{/each }}


